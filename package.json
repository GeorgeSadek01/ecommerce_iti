{
  "name": "ecommerce",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter backend dev",
    "dev:frontend": "pnpm --filter frontend start",
    "lint": "pnpm --filter backend lint",
    "test": "pnpm --filter backend test --if-present",
    "build": "pnpm --filter backend build --if-present"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  "scripts": {
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.4",
    "nodemon": "^3.1.14",
    "slugify": "^1.6.6",
    "stripe": "^20.4.1"
  }
}
